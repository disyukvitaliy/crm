%h2 Forgot your password?

.well
  = bootstrap_form_for(resource, as: resource_name, url: password_path(resource_name), layout: :horizontal, label_col: 'col-sm-2', control_col: 'col-sm-10') do |f|
    .form-group
      .col-sm-10.col-sm-offset-2
        =f.alert_message 'Please fix the errors below.', class: 'alert alert-danger'
    = f.email_field :email, placeholder: 'Email', autofocus: true
    .form-group
      .col-sm-10.col-sm-offset-2
        =f.submit 'Send me reset password instructions', class: 'btn btn-primary', wrapper: {class: 'has-warning'}
        =button_tag :Cancel, class: 'btn btn-default', type: :reset
    .form-group
      .col-sm-10.col-sm-offset-2
        = render 'devise/shared/links'